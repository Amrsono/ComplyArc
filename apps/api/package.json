{
  "name": "@complyarc/api",
  "version": "1.0.0",
  "private": true,
  "description": "ComplyArc — Backend API Package",
  "scripts": {
    "test": "pytest tests/ -v",
    "test:cov": "pytest tests/ --cov=app --cov-report=term-missing"
  }
}
